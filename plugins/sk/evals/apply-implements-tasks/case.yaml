schema_version: "1.1"
name: apply-implements-tasks
description: >-
  /sk:apply implements the change's tasks and ticks each one off as its
  specified behaviour lands.
tags: [core]
context:
  scaffold_script: fixture.sh
