schema_version: "1.1"
name: archive-blocks-open-question
description: >-
  A finished, otherwise-valid change whose delta still carries the
  open-question marker. /sk:archive must stop and name the question rather
  than merge an unresolved value into the living spec.
tags: [core]
context:
  scaffold_script: fixture.sh
