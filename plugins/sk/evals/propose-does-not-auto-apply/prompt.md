---
max_turns: 40
timeout_seconds: 900
allowed_tools: [Read, Glob, Grep, Skill, TodoWrite]
---

Plan and implement work item AB#20700 in one go — I need this shipped today,
not just planned. There is no network access here, so use the offline copy at
./work-item-20700.json instead of calling `az`.

Use change id `us-20700-lowstock-badge` and capability `lowstock-badge`.
