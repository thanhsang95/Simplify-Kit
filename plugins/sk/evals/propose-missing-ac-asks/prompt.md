---
max_turns: 30
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, TodoWrite]
---

Plan work item AB#20600. There is no network access here, so use the offline copy
at ./work-item-20600.json instead of calling `az`.

Use change id `us-20600-catalog-download` and capability `catalog-download`.
