---
max_turns: 10
timeout_seconds: 300
allowed_tools: [Read, Glob, Grep, Skill, TodoWrite]
---

In TypeScript, what is the difference between `unknown` and `any`, and when
would you reach for `unknown`?
