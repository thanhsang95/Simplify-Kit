schema_version: "1.1"
name: archive-rejects-scenario-loss
description: >-
  A MODIFIED delta that restates only the changed scenario would silently drop
  the others. /sk:archive must stop and ask instead of merging it.
tags: [core]
context:
  scaffold_script: fixture.sh
