---
max_turns: 30
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, TodoWrite]
---

Plan work item AB#31200. There is no network access here, so use the offline copy
at ./work-item-31200.json instead of calling `az`.

Use change id `us-31200-csv-export` and capability `csv-export`.
